node_modules/
dist/
.env
.env.*
!.env.example
.wrangler/
.DS_Store
lighthouse-reports/
.claude/settings.local.json
*.log
GOAL.md
CLAUDE.md

# Loose token file (not committed; the spec assumes secrets live in .env)
cf_api_token.txt

# Mailer Worker config — embeds the verified Email Routing destination.
# Operator copies workers/mailer/wrangler.toml.example to
# workers/mailer/wrangler.toml locally and fills in the address.
workers/mailer/wrangler.toml

# Operator-private docs (kept at /Users/cjohnson/Documents/agent-ready-poc-private/);
# never in public repo per iter-23 scrub.
docs/final-report.md
docs/cloudflare-dashboard-checklist.md
